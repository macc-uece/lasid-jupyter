apiVersion: v1
kind: Service
metadata:
  name: {{ template "lasid-jupyter.fullname" . }}
  labels:
    app: {{ template "lasid-jupyter.name" . }}
    chart: {{ template "lasid-jupyter.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "lasid-jupyter.name" . }}
    release: {{ .Release.Name }}
